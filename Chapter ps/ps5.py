class Para:
    def __init__(slf, name):
        slf.name = name

    def name():
        return slf.name

obj =  Para('John')
print(obj.name)

#nothing will change if you use "slf" or any word instead of self but  it is a convention to use "self" in python

